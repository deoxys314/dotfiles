# This file contains aliases which are useful for all instances of fish

# ls (a)ll (B) print unprintables (h) sizes in megabyte, et a;
#    (s) file block size (l) long format
alias lsa 'ls -aBhsl'

# it's happened enough times
alias :q exit
